---
// This page handles the PKCE exchange on the client side
---

<!DOCTYPE html>
<html>
<head>
  <title>Completing Login...</title>
  <style>
    body { 
      font-family: system-ui; 
      display: flex; 
      align-items: center; 
      justify-content: center; 
      height: 100vh; 
      margin: 0; 
      background: linear-gradient(135deg, #1e293b, #334155);
      color: white;
    }
    .spinner { 
      width: 40px; 
      height: 40px; 
      border: 4px solid #64748b; 
      border-top: 4px solid #06b6d4; 
      border-radius: 50%; 
      animation: spin 1s linear infinite; 
      margin-bottom: 20px;
    }
    @keyframes spin { 0% { transform: rotate(0deg); } 100% { transform: rotate(360deg); } }
  </style>
</head>
<body>
  <div style="text-align: center;">
    <div class="spinner"></div>
    <p>Completing your login...</p>
    <p id="status" style="color: #64748b; font-size: 14px;">Initializing...</p>
  </div>

  <script type="module">
    import { createClient } from 'https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2/+esm';
    
    const supabase = createClient(
      'https://mdhtpjpwwbuepsytgrva.supabase.co',
      'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Im1kaHRwanB3d2J1ZXBzeXRncnZhIiwicm9sZSI6ImFub24iLCJpYXQiOjE3NDgyMTQ0NTQsImV4cCI6MjA2Mzc5MDQ1NH0.T5eIZsKrpivj9Q4QniFhqOBGF_QU3f32wc0rcz4S-fA'
    );
    
    async function handleAuthExchange() {
      const status = document.getElementById('status');
      
      try {
        status.textContent = 'Processing authentication...';
        
        const urlParams = new URLSearchParams(window.location.search);
        const code = urlParams.get('code');
        
        if (!code) {
          throw new Error('No auth code received');
        }
        
        console.log('🔄 Attempting to get session...');
        status.textContent = 'Checking authentication session...';
        
        // First, let Supabase handle the callback automatically
        // The session should already be set by the time we reach this page
        const { data: { session }, error: sessionError } = await supabase.auth.getSession();
        
        if (session && session.user) {
          console.log('✅ Session found:', session.user.email);
          status.textContent = 'Authentication successful! Redirecting to dashboard...';
          
          // Simple redirect to dashboard
          setTimeout(() => {
            console.log('🔄 Redirecting to dashboard');
            window.location.href = '/dashboard';
          }, 1000);
          return;
        }
        
        console.log('🔄 No session found, trying manual code exchange...');
        status.textContent = 'Exchanging authentication code...';
        
        try {
          // Try manual code exchange as fallback
          const { data, error } = await supabase.auth.exchangeCodeForSession(code);
          
          if (error) {
            console.error('❌ Manual exchange error:', error);
            throw error;
          }
          
          status.textContent = 'Authentication successful! Redirecting to dashboard...';
          console.log('✅ Manual exchange successful:', data.user?.email);
          
          setTimeout(() => {
            console.log('🔄 Redirecting to dashboard');
            window.location.href = '/dashboard';
          }, 1000);
          
        } catch (exchangeError) {
          console.error('❌ Both automatic and manual auth failed:', exchangeError);
          status.textContent = 'Authentication failed. Redirecting...';
          setTimeout(() => {
            window.location.href = '/login?error=auth_failed';
          }, 2000);
        }
        
      } catch (error) {
        console.error('❌ Auth exchange error:', error);
        status.textContent = 'Something went wrong. Redirecting...';
        setTimeout(() => {
          window.location.href = '/login?error=unexpected';
        }, 2000);
      }
    }
    
    // Start the exchange process
    handleAuthExchange();
  </script>
</body>
</html>


