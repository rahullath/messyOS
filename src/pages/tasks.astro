---
import DashboardLayout from '../layouts/DashboardLayout.astro';
import { createServerAuth } from '../lib/auth/simple-multi-user';

// Require authentication
const serverAuth = createServerAuth(Astro.cookies);
const user = await serverAuth.getUser();

if (!user) {
  return Astro.redirect('/login');
}
---

<DashboardLayout title="Task Management - MessyOS" user={user}>
  <div class="space-y-8">
    <!-- Header -->
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-3xl font-semibold text-text-primary mb-2">Task Management</h1>
        <p class="text-text-secondary">
          Create, organize, and track your tasks efficiently with AI-powered insights
        </p>
      </div>
      <button 
        id="add-task-btn"
        class="flex items-center px-4 py-2 bg-accent-primary text-white rounded-lg hover:bg-accent-primary/90 transition-colors"
      >
        <svg class="w-5 h-5 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
        </svg>
        New Task
      </button>
    </div>

    <!-- Task Management Component Container -->
    <div id="task-management-root">
      <!-- Fallback content while React component loads -->
      <div class="card p-8 text-center">
        <div class="w-16 h-16 bg-accent-primary/10 rounded-full flex items-center justify-center mx-auto mb-4">
          <span class="text-2xl">📋</span>
        </div>
        <h3 class="text-lg font-semibold text-text-primary mb-2">Task Management</h3>
        <p class="text-text-secondary mb-6">
          Advanced task management system is loading. Create, organize, and track your tasks with AI-powered insights.
        </p>
        
        <div class="grid grid-cols-1 md:grid-cols-3 gap-4 max-w-md mx-auto mb-6">
          <div class="p-3 bg-surface-hover rounded-lg">
            <div class="text-2xl mb-1">🎯</div>
            <div class="text-sm font-medium text-text-primary">Smart Categorization</div>
            <div class="text-xs text-text-muted">AI-powered task organization</div>
          </div>
          <div class="p-3 bg-surface-hover rounded-lg">
            <div class="text-2xl mb-1">⚡</div>
            <div class="text-sm font-medium text-text-primary">Energy Matching</div>
            <div class="text-xs text-text-muted">Tasks matched to energy levels</div>
          </div>
          <div class="p-3 bg-surface-hover rounded-lg">
            <div class="text-2xl mb-1">📊</div>
            <div class="text-sm font-medium text-text-primary">Progress Tracking</div>
            <div class="text-xs text-text-muted">Time tracking and analytics</div>
          </div>
        </div>
        
        <div class="text-sm text-text-muted">
          If this message persists, please refresh the page or check the browser console for any loading errors.
        </div>
      </div>
    </div>
  </div>
</DashboardLayout>

<script>
  // Add task button functionality
  document.getElementById('add-task-btn')?.addEventListener('click', () => {
    alert('📋 Advanced task management system is being initialized. This will integrate with the intelligent task management features including AI task creation, energy-aware scheduling, and comprehensive time tracking.');
  });

  // Try to load React component
  // For now, we'll keep the fallback content
  // In a full implementation, you would load the React component here
  console.log('Task management page loaded. React component integration pending.');
</script>